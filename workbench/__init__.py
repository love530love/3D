"""Workbench: unified human control surface + decision dashboard for the 福彩3D project.

Pure standard library. Does not mutate data on its own; it orchestrates the
existing, separately-governed pipeline scripts. High-risk operations (rollback,
data ingestion) are gated and require explicit confirmation.
"""

__version__ = "0.1.0"
