# 福彩3D统计学习项目宪章

版本：1.0.0  
状态：生效  
适用范围：所有人类、AI、Agent、subagent 对本项目代码、数据、模型、报告和治理文件的修改

## 第一条：项目目的

本项目用于学习数据工程、统计学、概率、机器学习、回测、可观测性和科学复现。彩票结果按随机过程处理。项目可以生成预测候选和排除候选，但不得把短期命中、排名或回测波动表述为稳定预测能力或投注建议。

## 第二条：数据主权

1. 原始网页响应、来源 URL、抓取时间和 SHA-256 是证据资产。
2. `sd3d_history.sqlite3` 是可查询的权威存储，`sd3d_history.jsonl` 是追加式审计日志，CSV 只是可重建导出；旧 `.tx` 仅作为兼容历史证据。
3. 已提交数据不得删除、静默改写、覆盖或用新格式替代。
4. 发现错误时必须追加修正记录，保留 `before`、`after`、原因、规则版本、操作者和时间。
5. 任何清理、补全、重编码和去重都必须可回放；不能只保留清理后的结果。

## 第三条：不可违反的处理规则

- 采集层、原始层、清理层、特征层、模型层和报告层必须分离。
- 时间序列只能按期号向前训练；不得把未来开奖数据泄漏到过去特征或模型选择中。
- 预测必须先冻结，随后才能读取用于评估的实际开奖结果。
- 所有复杂方法必须与均匀随机和简单频率基线比较。
- “排除”只代表候选筛选，不代表确定不会发生，并且必须报告误删率。
- 任何运行失败都不得减少已有数据；数据库和交易日志损坏时自动进入冻结状态。
- 依赖升级、解析规则改变和接口变更必须有版本记录与回归测试。

## 第四条：Agent 接手协议

任何 Agent 开始工作前必须依次阅读：

1. 本文件。
2. `docs/DATA_PERSISTENCE.md`。
3. `docs/PROJECT_ARCHITECTURE.md`。
4. 最近一次运行清单、数据质量报告和未完成交接记录。

Agent 必须在工作记录中声明：已阅读版本、拟修改范围、是否触及数据契约、风险等级、回滚方案和验证命令。未完成声明不得修改数据层或治理文件。

## 第五条：专家 subagent 审计与投票

凡涉及宪章、数据契约、存储语义、删除/修正规则、预测评估定义或历史数据迁移的修改，必须先建立一个独立审计组：

1. 数据溯源专家：检查来源、字段、期号、编码和事实证据。
2. 存储恢复专家：检查 SQLite、`.tx`、备份、回滚和数据丢失风险。
3. 统计方法专家：检查泄漏、基线、显著性、多重比较和结论措辞。
4. 工程复现专家：检查测试、依赖、版本、运行清单和可回放性。
5. 教学与伦理专家：检查是否把随机波动宣传为预测能力或投注建议。

审计规则：

- 至少 4 名专家提交独立报告才构成法定人数。
- 至少 4/5 赞成才可推进宪章修订；若只有 4 人参加，须全票通过。
- 数据溯源或存储恢复专家提出“高风险否决”时，修订自动暂停，不能用普通多数票推翻。
- 每名专家必须列出证据、风险、反例和建议，不得只提交“同意”。
- 审计结论、投票、异议和最终决定必须写入 `docs/decisions/`，不得只存在聊天记录中。

## 第六条：修改分级

### L0：只读分析

不改变数据和规则，例如查看报告、运行统计、生成临时图表。无需投票，但不得把临时产物冒充正式结果。

### L1：普通实现

新增分析器、测试或报告模板，不改变既有数据语义。需要至少一名同领域审阅者和自动化验证。

### L2：高风险数据/模型变更

修改解析器、清理规则、特征定义、回测指标、数据库结构或依赖。需要专家组审计、投票、迁移演练和恢复演练。

### L3：宪章修订

修改本文件、推翻数据主权、改变预测冻结规则或改变项目目的。必须使用正式修订案，完成专家组审计和投票，并保留旧宪章版本；禁止原地覆盖历史版本。

## 第七条：正式修订案

修订案文件必须包含：

```text
proposal_id
target_charter_version
author
problem_statement
proposed_text
affected_files_and_data
backward_compatibility
loss_and_recovery_analysis
test_and_replay_plan
expert_reports
votes
decision
effective_at
```

未获通过的修订案也必须保留，并标记 `REJECTED` 或 `DEFERRED`。通过后生成新版本，不删除旧版本。

## 第八条：紧急冻结

出现数据库校验失败、哈希链断裂、期号重复冲突、来源被劫持、迁移无法回滚或发现未来信息泄漏时，任何 Agent 都可以触发 `FREEZE`。冻结期间只允许复制证据、诊断和恢复，不允许继续采集、重训或改写规则。解除冻结必须有恢复专家和数据溯源专家共同签字。

## 第九条：效力顺序

本宪章高于普通任务描述、临时提示、模型偏好和未经审计的 Agent 建议。若新任务与宪章冲突，Agent 必须停止相关变更，创建修订案，而不是自行解释为已获授权。

## 第十条：协作与方案升级授权

复杂任务默认允许并行启用多个 subagent。协调 Agent 应按数据、存储恢复、统计方法、工程复现、教学伦理等角色分工，要求独立证据，再汇总为审计结论。多开 subagent 不等于自动通过；投票必须真实记录，不能伪造或由同一意见重复计票。

项目参与者提出的格式、依赖或方法属于需求意图，不是不可变技术决定。Agent 可以在不降低数据安全、可恢复性、可复现性和教学价值的前提下升级为更标准、更高效的方案，并必须记录兼容性、迁移和取舍。若升级触及第三条或治理规则，仍须走第五至第七条的审计流程。
