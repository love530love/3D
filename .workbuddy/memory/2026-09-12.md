# 2026-09-12

## 执行 run_update_cycle.py 日常更新 + 回测（用户："目前可以更新最新结果并做回测了"）
- 在 Agent 模式、用项目 `.venv` 跑 `run_update_cycle.py`（后台 1m20s，exit 0，Update cycle PASS，4 单测 OK）。
- 抓取：Fetched 1 / inserted 1 → `draws` 7750→**7751 行**，新增期 **2026244**（实际号码 **654**，开奖日 2026-09-11）。
- 盲评：3 期冻结预测全部 completed（2026215=172、2026216=935、2026244=654），**精确命中 0/3**；2026244 最佳候选位置命中=1（百位 6 命中）。
- 质量门禁 PASS；泄漏扫描 PASS（scanned=3）；证据大脑 verdict=**NO_STABLE_PREDICTIVE_EDGE_OBSERVED**（选定 uniform）；模型门禁=**BASELINE_REQUIRED**。
- 回测（7251 tested vs uniform）：`position_frequency` 差异 +0.00303，Bootstrap 95% CI [-0.00083, +0.00607]（跨0）；`recent_position_frequency` 差异 +0.00028，CI [-0.00317, +0.00331]（跨0）→ 均无稳定优势。概率评分：uniform log_loss 2.3026 < smoothed 2.3039（基线略优）。随机性 χ² p≈0.44、lag1 相关≈0.006、置换峰值 p=1.0。
- 新冻结：`frozen-2026245-98ef8df6961e4786beb1c98b50c6e35b.json`（target=2026245）；**candidates 与 2026244 完全相同**（648/640/647/668/660/667/618/610/617/748）——单期增量未改变各位置 top-3 数字。
- 刷新 `extract_predictions.py` → `predictions/extracted_predictions.csv`：4 期预测、3 已开奖、精确命中 0。
- DB sha256=`9db8355f39e7e4db0dcb97f9c6468c7270114fc760ba443460d00a4b25062c38`；jsonl tail_hash=`c90cfeb6…`（7760 行）。

## 发现流水线排序瑕疵（待修，L1）
- `run_update_cycle.py` 在 `record_outcomes` **之前**调用 `run_pipeline`（含 `analyze_outcomes`/`evidence_brain`），导致 `reports/outcomes-analysis-latest.json` 与 `brain-decision-latest.json` 内嵌 `outcome_analysis` 落后一周期（显示 completed=2，实际 DB=3）。DB 本身正确。
- 修复建议：把 `record_outcomes` 移到 `run_pipeline` 前，或在其后补跑 `analyze_outcomes`+`evidence_brain`。属 L1，待用户确认后实施并验证（重跑应得 completed=3）。

## 可追溯化
- 新增 `docs/HANDOFF-2026-09-12.md`（衔接 HANDOFF-2026-09-11 的 next_safe_action）；`git commit` 仅提交该文档（运行产物均被 gitignore，未提交）。

## 修复流水线排序瑕疵（用户："需要 改掉并重跑验证"）
- 用户确认后实施 L1 修复：将 `run_update_cycle.py` 的 `record_outcomes.py` 调用移到 `run_pipeline.py` **之前**（原顺序 validate→pipeline→record_outcomes 导致 analyze_outcomes/evidence_brain 读到的盲评表落后一周期）。
- 重跑验证：`Outcome archive: 3 completed; 1 pending; 4 total`、`Outcome analysis: 3 completed records`、`outcomes-analysis-latest.json` 现 `completed_total=3, exact_hits=0, mean_max_position_hits=0.333`；`Leakage scan PASS; scanned=4`；单测 4 passed；`Update cycle PASS`。
- 额外修复效果：本次重跑 compare 阶段补齐了上一轮缺失的 `frozen-2026244-…-comparison.json`（因上次 compare 时 2026244 尚未入库）。
- 幂等确认：抓取 `No new draw rows`，未重复冻结（2026245 pending → 未生成 2026246）。
- commit `3ea0414`（仅 run_update_cycle.py，+6/-3）；HANDOFF-2026-09-12.md 增补修复记录。运行产物/新 comparison 文件仍被 gitignore。

## 方法论文 critiqu（用户："预测方式是否太过单一，不足以说明'穷尽所有预测手段皆证明不可预测性'"）
- 用户指出"穷尽所有预测手段皆证明不可预测性"这一叙事站不住。核实代码后结论：**critique 成立**。
- 关键事实：正式判定链 `models_sd3d.REGISTRY` 仅 3 模型（uniform + position_frequency + recent_position_frequency）；`compare_models_stats` 的 Bootstrap 对比只含 **2 个 challenger**（均位置频率变体）；`model_gate`/`evidence_brain` 的 `challenger_comparison` 也仅这 2 个 → 正式结论仅建立在单一方法家族上。
- 但 `model_screening.py` 已有 **7 模型**（含 laplace 平滑、recent_50/100/300 窗口、一阶马尔可夫 `markov_position_laplace`），却被标注"描述性筛查"、硬编码 `stable_advantage_observed=False`，且**未接入**正式 gate/brain 判定。属"已实现但未接线"。
- 更根本：经验上无法"证明不可预测"（证据缺失≠不存在）；最强可辩护结论是"多家族严格检验下一致未找到可利用结构"。项目现用 `NO_STABLE_PREDICTIVE_EDGE_OBSERVED` 措辞本身正确克制，问题在支撑电池太窄 + 叙事易误读。
- 给出三方向待用户拍板：A 仅改叙事（L1 文档）；B 把 model_screening 已有 6 challenger 接进 REGISTRY+Bootstrap+brain 并加多重比较校正（性价比最高，中低风险）；C 真扩充家族（周期/派生特征/ML，L2 需专家审计）。我倾向 B→C，并以 A 钉死话术。截至本回合用户尚未选定方向。

## L2 审计 + 多家族电池实现（用户："按最权威的方法去做"）
- 采用宪章第五条 L2 流程：写正式提案 `docs/decisions/MODEL-FORMAL-INTEGRATION-PROPOSAL.md` → 五专家独立审计 + 投票；无 ≥4/5 通过不得动代码。
- v1.0 第一轮审计 2 赞成 / 3 暂缓（统计/工程/教学），DEFERRED。按 3 项暂缓修订 v1.1（承认并定稿修复 `model_gate.py:22` 硬编码 bug；预登记 BH-FDR 的 m/经验 p/门槛；加 `screening_adapter`+契约单测+命名统一表；重写认识论为负向结论 vs 正向证明 + 残余不确定性清单）。
- v1.1 第二轮审计 **5/5 APPROVE** → 批准，授权实施（commit `b9227c9`）。
- 实施要点：`model_gate` 修复硬编码锚点改为按各 challenger 自身 log_loss + 要求 corrected_p<0.05；`probability_metrics` 加 `empirical_p_value`/`benjamini_hochberg`；`compare_models_stats` 电池扩至 7 challenger 输出 raw_p/corrected_p；`models_sd3d` 加 `screening_adapter` 把 model_screening 的 7 scorer 接进 REGISTRY（REGISTRY 现 8 模型）；新增 3 个测试文件。
- 验证 `run_pipeline.py` 全绿：泄漏 PASS、13/13 单测 OK、`NO_STABLE_PREDICTIVE_EDGE_OBSERVED`、`BASELINE_REQUIRED`；7 个 challenger 的 BH-FDR `corrected_p` 全部 = 1.0。结论从"2 同质频率 challenger"升级为"多家族 + Bootstrap + 多重比较校正下一致未观测到优势"的负向证据。
- 关键纪律：宪章 L2 下，提案批准前**零代码改动**；所有修订先落文档再实施。冻结预测路径 `predict_sd3d.py` 不读 REGISTRY，故扩 REGISTRY 不影响既有冻结预测。

## 新建 workbench（人类友好决策大屏 + 一键控制面）
- 诉求：用户要"联结界面，一键操作所有功能与回测，人类才好决策"，功能完善、稳健、可扩展，可打印可交互。对应架构 A 的可视化+控制层。
- 纯标准库实现（无外部依赖）：`workbench/` 包 = `registry.py`(21 个功能注册表，含风险等级 L0/L1/L2 与确认标记) + `engine.py`(subprocess 编排、collect_state、git 版本/回滚) + `dashboard.py`(自包含 HTML 决策大屏 + print_summary) + `serve.py`(ThreadingHTTPServer，/api/state、/api/run 后台任务、/api/task 轮询、受控 /api/rollback) + 根目录 `workbench.py` CLI(`list/run/backtest/decisions/state/print/dashboard/serve/rollback`)。
- 大屏含 KPI 卡、功能网格(一键运行)、回测视图、决策审计列表、回溯时间轴、报告明细；`@media print` 切浅色可打印 PDF。静态 `dashboard.html`(24KB) 与交互服务端 `--serve`(默认 8787) 同源渲染。
- 关键修复：`registry.resolve_args` 原用 `"DB" in "{DB}"` 替换导致路径残留花括号 → sqlite 打不开；改为 `"{"+tok+"}"` 整段替换。测试时还踩到"旧 server 仍占端口 8799 导致新代码未生效"的坑，换端口 8801 复测通过。
- 验证：CLI `list/print/dashboard` 正常；`run validate/analyze/drift` rc=0；服务端 `/api/run`+轮询跑通，`/api/rollback` 无 confirm 时正确 ABORT。回滚始终先安全快照再 `git checkout`，L2 需确认。
- 宪章合规：workbench 不自行改数据，只编排既有受治理脚本；高风险 rollback 强制确认。属 L1 工具（未提交 git，待用户确认）。

## 多预测方式共存底层 + 功能页参数表 + 多方法对比偏差（用户："先设计底层多种预测方式共存..."）
- **底层共存层 `workbench/forecasts.py`（已完成，核心诉求）**：以 `models_sd3d.REGISTRY` 为天然基础，单独的分析缓存 `workbench/forecasts_cache.sqlite3`（**不碰权威 DB**，宪章 Article 2，L1 合规）。`generate_for_period` 对每期用**严格时间前**数据（int(p)<int(period)）生成 8 种方法预测；`compare_period` 对比实开；`compare_window` 近 N 期严格窗口聚合；偏差度量 = 精确命中 / 位置命中(0-3) / 数字偏差Σ|实际位−预测位| / log-loss(有分布时)。`build_report` 汇总成 `reports/multi-method-latest.json`。
- **功能页参数表**：`registry.py` 给 backtest/predict/diagnose/compare/evaluate/probability 加 `params`（name/flag/type/default/help），并新增 `multi_method` 功能（params: last_n/top_k/alpha）。基础 `args` 只留路径 `--db/--out`，数值旋钮全部走 params，避免重复 flag。
- **多方法对比偏差展示**：`engine._run_multi_method` 解析参数→`build_report`→写 `reports/multi-method-latest.json`→collect_state 纳入 `multi_method`；`dashboard.py` 新增"多方法对比"Tab（最近一期各方法 vs 实开 654 的命中/位命中/数字偏差/log-loss 表 + 近 N 期聚合排序表 + 免责声明）；功能卡内渲染参数输入，运行按钮收集 `input[data-flag]` 以 `{id, args}` 发 `/api/run`。
- **服务端透传**：`serve.start_task/POST /api/run` 现接受 `args` 透传给 `engine.run_function` 的 `extra_args`（普通脚本追加到 cmd，multi_method 走专用分支）。CLI 新增 `multi_method` 子命令 + `run <id> [args...]` 透传。
- **端到端验证(端口 8811)**：`/api/state` 200 且 multi_method 存在、22 功能；`multi_method --last-n 8 --top-k 5 --alpha 0.2` rc=0 且报告 config 同步为 {8,5,0.2}；`predict --top-k 10`、`diagnose --permutations 100 --seed 42` rc=0（参数透传生效）；`/api/rollback` 无 confirm 正确 ABORT。静态 `dashboard.html` 34KB 含新 Tab 与参数表单。
- 教训复现：后台起 server 时若同时用 `run_in_background` 又加 `&`，子进程会随包装 shell 退出被杀；应只用 `run_in_background=true` 不带 `&`。属 L1 工具扩展（未提交 git，待用户确认）。
