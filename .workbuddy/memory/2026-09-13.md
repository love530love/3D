# 2026-09-13

## 数据更新：权威库补齐新一期（用户纠正"最新是 549"）
- 大屏早前显示最新一期停在 2026244/654，用户指出最新实际应为 549 → 库缺 2026245。
- `workbench.py run fetch` 增量抓取 1 期：2026245 = **549**（2026-09-12），draws 7751→**7752**。属 L1 追加（不可变库只追加），已写入 sqlite3 + jsonl 审计。
- 随后重跑 backtest（7252 期）与 multi_method，大屏最新实开正确显示为 549。结论不变：各方法近 12 期精确率 ~0%，无稳定优势（NO_STABLE_PREDICTIVE_EDGE）。

## 大屏新增两块：下期预测 + 历史预测VS实开（用户："大屏上应该再新增一个 新一期2026246的预测，还有过去一期的预测VS实开"）
- `workbench/forecasts.py`：`next_period(db,top_k,alpha)` 用全部更早数据预测下一期（period=最新+1，无实开，纯盲评）；`historical_compare` 复用 `compare_period`；`build_report` 新增 `history_offset`（默认2）→ 输出 `next_period` 与 `historical_compare`，并把所有 compare 改 `use_cache=False` 以保证当前参数生效。
- `workbench/registry.py`：`multi_method` 功能加参数 `history_offset`（flag `--history-offset`，默认2 = 最新前两期，如2026243）。
- `workbench/engine._run_multi_method`：解析 `--history-offset` 并传 build_report，日志补"下期预测/历史对照"两行。
- `workbench/dashboard.py`：multiMethodHtml 新增两板块——① 下期预测(2026246)：各方法 Top 候选 + 百/十/个位分布 Top3；② 历史预测 VS 实开(如2026243/219)：命中/位命中/数字偏差/log-loss 表。
- `workbench.py` CLI：`multi_method` 子命令加 `--history-offset`，`run <id>` 已支持透传额外参数。
- 验证：next_period=2026246(trained_on 2026245, 8方法)；historical=2026243 actual=219(8方法，metrics 正常)；dashboard.html 含"下期预测"。属 L1 扩展（未提交 git，待用户确认）。

## 待办/提示
- 这次数据更新(2026245/549)尚未提交 git；若用户要可恢复版本点，需提交 sd3d_history.sqlite3?（注意 .gitignore 忽略该文件——要提交须先改忽略或 `git add -f`）。

## 大屏新增「历史分析」标签页（人类视觉管理：用户"完善人类视觉管理功能"）
- 新增 `workbench/history_stats.py`（L1，只读权威库）：`load_all` 解析 values_json→派生每期 和值/跨度/奇偶/大小/组选类型；`sum_distribution`(0-27)/`span_distribution`(0-9)/`position_frequency`/`position_omission`(全历史)/`parity_size_stats`(奇偶/大小/质合)/`type_stats`(豹子/组三/组六)/`hot_cold`；`prediction_rolling` 复用 forecasts 严格时间前向预测做近N期×各方法命中(✓/✗)滚动对照；`build_report` 聚合+disclaimer。
- `workbench/registry.py`：新增功能 `history_stats`(脚本 `__history_stats__`, L1, 参数 `--window` 60 / `--pred-window` 20 / `--top-k` 10 / `--alpha` 0.1)。
- `workbench/engine.py`：`run_function` 特判 `__history_stats__`→`_run_history_stats`，写 `reports/history-stats-latest.json`；`collect_state` 载入 `history_stats`。
- `workbench/dashboard.py`：新增 tab「历史分析」+ `historyHtml()` + 内联SVG助手 `svgLine`/`svgBars`/`catBars`（保持自包含、可打印、无外部库）；含 历史开奖原始表 / 和值走势(折线+均值) / 和值&跨度分布直方图 / 各位频率+遗漏 / 奇偶大小质合占比 / 组选类型 / 冷热号 / 历史预测滚动对照表。
- `workbench.py` CLI：加 `history_stats` 子命令；新增根脚本 `history_stats_compare.py`（对齐 `multi_method_compare.py`）。
- 验证：history_stats 经 engine 生成报告(60期/20期预测对照8方法)、node --check 通过、Node 运行时渲染 historyHtml 无错(3个SVG/60行类型徽章/10冷热chip/160预测单元格)、serve /api/state 含 history_stats。属 L1 扩展，未提交 git（reports/* 已忽略可重建）。
- 注：`position_omission` 用全历史计算（"遗漏"需全量），其余频率/统计用 window 窗口。
