# 2026-09-11

## 安装/构建：仓库架构知识库技能组合
- 用户需求：组合「文件遍历与读取 + AST 静态解析 + 代码 RAG 向量检索」，一次性构建仓库架构知识库，跨会话无需重复讲解项目结构。
- 从本地 marketplace 安装（已做安全审计）：
  - `llm-wiki`（本地持久化 wiki，Karpathy 增量模式，用于跨会话架构文档；默认路径 `~/.workbuddy/wiki-knowledge/`）
  - `markitdown-skill`（非文本文件→markdown 读取）；pip 包 `markitdown==0.1.1` 装在隔离 venv `~/.workbuddy/skills/repo-arch-kb/.venv`（共享 venv 有 dotenv/requests 冲突，故独立 venv）。
- 新建技能 `repo-arch-kb`（`~/.workbuddy/skills/repo-arch-kb/`）：单脚本 `scripts/codekb.py`，子命令 `scan`/`build`/`query`。
  - 文件遍历：Glob 式 walk + DEFAULT_IGNORES；Python 用 `ast` 抽类/函数/方法/签名/文档/导入，其他语言正则尽力抽取。
  - AST：见上；非 Python 为 regex 尽力而为。
  - 代码 RAG：`HashingEmbedder`（默认，384 维，词/字符 n-gram 哈希，**支持中英文**，无需下载模型）+ 可选 `sentence-transformers`（`--embedder st`）；numpy 余弦相似度；索引存 `.codekb/`（vectors.npy + chunks.jsonl + meta.json + ARCHITECTURE.md + CODEMAP.md）。
  - embedder 已修 CJK 支持（中文查询命中中文 docstring，得分 ~0.7）。
- 已在 `F:\PythonProjects1\3D` 实际构建：26 文件 / 2191 LOC / 97 chunks；中文与英文查询均命中相关文件。
- 已 seed 架构文档到 `~/.workbuddy/wiki-knowledge/raw/code-arch/`（ARCHITECTURE.md + CODEMAP.md）+ index.md，供 llm-wiki 跨会话复用。
- 用法：用 `$SKILL_DIR/.venv/Scripts/python.exe scripts/codekb.py build --root <repo>` 构建；`query` 检索。SKILL.md 含完整说明。

## 福彩3D 预测结果提取
- 用户问"怎么从项目里提取预测结果"。预测结果分布：
  - `predictions/frozen-*.json`（kind=frozen_prediction）：**预测侧**，`candidates`=候选3位号码(10个)、`position_rankings`=各位频率排名、`target_period`/`model`。
  - `*-comparison*.json` 与 `reports/outcomes-latest.json`：预测vs实际（实际开奖在 sqlite `draws` 表 `values_json` 字段，fields[1] 是号码）。
  - `outball.csv`=开奖结果(期号/百位/十位/个位)；`count_result.csv`/`prob_result.csv`=统计。
- 新建只读提取脚本 `F:\PythonProjects1\3D\extract_predictions.py`（纯 stdlib）：扫描 `predictions/frozen-*.json`（排除 comparison），关联 `draws` 表算 exact_hit/max_position_hits，导出 `predictions/extracted_predictions.csv`（含 all_candidates/actual/exact_hit 列）。运行：`python extract_predictions.py`。已实跑：2 期预测，实际 172/935，均未命中。

## 补：阅读项目治理文档 + 运行环境纠正
- 用户问"docs 你看了吗"——此前**未读** `docs/` 与 `PROJECT_CHARTER.md`，本轮已通读全部 11 篇 docs + `docs/decisions/` 4 文件 + 根宪章。
- 关键结论：之前给的"增量拉取"说法与 `DATA_PERSISTENCE.md`/`QUICKSTART.md` 一致（正确）。
- ⚠️ **运行环境偏差**：文档要求所有命令走项目 `.venv`（`\.venv\Scripts\python.exe`，Python 3.14.0，纯 stdlib，`requirements.txt` 为空）；我前面用了管理版 Python。后续改用 `.venv` 以符合项目约定。已验证 `.venv` 可跑全部脚本。
- 治理自检：提取器/`.codekb` 均为只读+CSV导出/代码知识库，未碰 `draws` 表/JSONL/历史报告，未违反数据主权(第二条)与分层(第三条)，属 L0/L1 低风险。`decisions/` 两份提案仍 DRAFT/禁止自动实施，模型变更当前无授权项。
- 后续动数据层前，按宪章第四条先写"接手声明"（已读版本/范围/风险/回滚/验证命令）。

## run_update_cycle.py 全量跑 + 泄漏扫描 BUG 修复
- 用户实跑 `run_update_cycle.py` → 在 `scan_leakage.py` 处 FAIL（scanned=9）并 STOP，导致 `record_outcomes.py` / `predict_outcomes` / `predict_sd3d.py` 未执行。
- **根因**：`run_update_cycle.py`(L25) 与 `scan_leakage.py`(L24) 都用 `glob("frozen-*.json")`，而 `compare_prediction.py` 的输出命名是 `frozen-<period>-<hash>-comparison.json`，也匹配 `frozen-*.json`。
  - 后果1（嵌套螺旋）：每次 cycle 把 comparison 文件再喂给 compare_prediction → 多一层 `-comparison`，用户跑约4次→4层嵌套。
  - 后果2（误报泄漏致中止）：scan_leakage 在 comparison 文件里看到 `actual` 字段 → 报 "frozen artifact contains actual outcome" → FAIL → 整条 pipeline STOP。
- **修复（L1，低风险，不改数据语义）**：两处 glob 加过滤 `if "-comparison" not in p.name`。已实跑验证：`scan_leakage.py` 现报 `PASS; scanned=2`。
- **待清理垃圾**：predictions/ 下 2 层及以上嵌套 comparison 文件（共 5 个，`*-comparison-comparison*.json`）为 bug 产物，1 层 `*-comparison.json` 是合法对比产物（可保留）。删除前需用户确认（宪章第二条不得静默删数据）。
- **现状**：2 个真实冻结预测 `frozen-2026215/2026216-*.json` 完好且通过扫描。修复后重跑 `run_update_cycle.py` 应可走完（record_outcomes + 冻结下一期）。

## 全量修复 + 完整跑通（用户授权"①+② 你帮我跑"）
- 已确认同款 bug 共 4 处 `glob("frozen-*.json")`：`run_update_cycle.py`(已修)、`scan_leakage.py`(已修)、`record_outcomes.py`(L30, **本次修复**)、`extract_predictions.py`(L71-72 自带 `-comparison` 跳过 + kind 检查，无需改)。
- 修复 `record_outcomes.py` L30 加 `if "-comparison" not in p.name` 过滤（L34 读 `run_id` 会因 comparison 文件无 run_id 抛 KeyError）。
- 删除 5 个嵌套垃圾 comparison 文件（`rm predictions/*-comparison-comparison*.json`），保留 2 真实冻结 + 2 个 1 层合法 comparison + extracted_predictions.csv。
- 重跑 `run_update_cycle.py` → **Update cycle PASS (exit 0)**。关键结果：
  - 全 pipeline 阶段 PASS；leakage scanned=2 PASS；evidence brain=NO_STABLE_PREDICTIVE_EDGE_OBSERVED；unittest OK。
  - `record_outcomes`：2 completed / 0 pending / 2 total（入库 prediction_outcomes 表）。
  - `predict_sd3d` 冻结下一期：`frozen-2026244-ed9b5270424649d580af68dfd6eae485.json`（target=2026244, cutoff=2026243, model=position_frequency_baseline_v1, 10 candidates）。
- 验证 predictions/：3 个真实 frozen（2026215/2026216/2026244，均 kind=frozen_prediction、含 run_id、10 candidates），无嵌套。
- **根因结论**：`compare_prediction.py` 输出名 `frozen-*-comparison.json` 也匹配 `frozen-*.json` glob，导致多脚本误处理 comparison 文件（嵌套 + 误报泄漏 + KeyError）。已通过三处 glob 过滤闭环修复。建议长期可把 comparison 产物迁到 `predictions/comparisons/` 子目录彻底隔离（待定，属 L2 较大改动，本次未做）。

## 实跑 fetch_sd3d.py 结果核验
- 用户在 `.venv` 下运行 `python fetch_sd3d.py` → "No new draw rows; existing SQLite and transaction log were preserved."（幂等正确）。
- 实测 DB 真相：`draws` 表 **7750 行**，max period=**2026243**，最新 draw_date=**2026-09-10**。即库已补到昨日，今日(09-11)期 2026244 尚未开奖/源站未放出 → 无新行属正常。
- ⚠️ `DATA_PERSISTENCE.md` 写的"7720行/到2026213"已过时（8-12 旧数据），文档不等于当前库。
- ⚠️ 文档 vs 实现漂移：宪章/架构文档写的核心契约是 `period,draw_date,d1,d2,d3` 分列，但**实际 `draws` 表无 d1/d2/d3/draw_date 列**，号码+日期都在 `values_json`（JSON 数组）里，如 `["2026243","2 1 9",...,"2026-09-10"]`。`extract_predictions.py` 读 `values_json` 取号码，与真实 schema 对齐。动数据层须注意此 schema。

## 3D.py 性质确认（用户问"3D.py 是干什么的"）
- 根目录 `3D.py` 是**游离/实验性** sklearn 决策树模板：读 `data.csv`(feature/target 两列) 训练 DecisionTreeClassifier、交叉验证、评估、存 `best_model.pkl`、画图 + 风险提示。
- 与真实管线**无关联**：全项目无 `import 3D`、不在 run_pipeline/run_update_cycle 中；读占位 `data.csv` 而非 sqlite `draws` 表。
- **当前跑不起来**：`data.csv` 为 0 字节(空)→read_csv 后缺列即崩；且需 pandas/sklearn/matplotlib/joblib，而 `.venv` 纯 stdlib(requirements.txt 空)→缺依赖。
- 治理风险：若误用于真实彩票数据会用 `train_test_split` 随机切分时序(违反宪章第三/四条禁随机切分)、无基线对比/无泄漏扫描/无冻结盲评——正好是宪章禁止的反模式。定性：死代码/早期草稿。
- 处置待定：归档到 scratch/ 或删除均须用户确认（宪章第二条不得静默删数据）。若要正经 ML challenger 须走 MODEL_PROPOSAL_TEMPLATE + 专家审计。
- **已归档（用户授权"挪到 scratch/ 归档"）**：`git mv 3D.py scratch/3D.py`（保留历史，staged rename 未 commit），空占位 `data.csv` 一并 `mv scratch/data.csv`。根目录已无 3D.py；`data.csv` 全项目仅 3D.py 引用故可同挪。
- ⚠️ 顺带发现 git 工作树有**非本次会话产生的未提交改动**：`run_pipeline.py` 与 `fetch_sd3d.py` 处于 modified 状态（本次我只改了 run_update_cycle/scan_leakage/record_outcomes）。按宪章交接清单属"未提交改动"，未触碰，仅提示用户。
- **可追溯化（用户要求"把所有操作都变成可追溯的"）**：
  - 新增 `docs/HANDOFF-2026-09-11.md` 交接记录（遵循 AI_MEMORY_AND_HANDOFF 最小格式）：含 charter_version=1.0.0、current_data_range(draws 7750, 2004001→2026243)、db_sha256、tx_tail event_hash、操作日志(7项含 before/after/验证/风险等级)、回滚方案、复现命令。
  - `git commit 31e5fd7`（main）：9 files changed, 508 insertions, 5 deletions。含本次 3 处 glob 修复 + extract_predictions.py + scratch 归档(3D.py git mv) + 既有未提交 WIP(fetch_sd3d 二进制写入修复、run_pipeline 接入 model_screening + model_screening.py)，出处已在交接记录标注。
  - **未提交**（按数据主权/本地记忆不外泄刻意排除）：`.workbuddy/ .codex/ .codekb/ backups/ docs/decisions/MODEL-SCREENING-AND-BRAIN-V2-PROPOSAL.md`(DRAFT)。git status 现仅余这些 ?? 项。

## README.md 新人向完善（用户："把 readme 完善一遍，让新手也能看懂操作流程和文件/命令的作用/效果/结果"）
- 先逐脚本核实真实产出路径（避免凭记忆写错）：`fetch_sd3d.py`→sqlite `draws`+`sd3d_history.jsonl`+`raw_snapshots/*.html`；`validate_sd3d.py`→`reports/quality-latest.json`；`record_outcomes.py`→sqlite `prediction_outcomes` 表+`reports/outcomes-latest.json`；`predict_sd3d.py`→`predictions/frozen-<target>-<run_id>.json`；`scan_leakage.py`→`reports/leakage-latest.json`；`evidence_brain.py`→`reports/brain-decision-latest.json`；`model_gate.py`→`reports/model-gate-latest.json`；`extract_predictions.py`→`predictions/extracted_predictions.csv`。
- 重写 README 为 12 节新人指南：环境准备(.venv Python3.14 纯 stdlib，requirements.txt 空)、两条主命令、端到端数据流图、命令速查表(作用/效果/产出/失败时)、数据存放与 Git 边界、结果解读(verdict/model_gate/leakage/quality)、治理红线、改代码前必读、可追溯性(引用 HANDOFF-2026-09-11.md 与复现命令)、FAQ、目录速览。
- 核实：`.venv` 存在且为 Python 3.14.0；`requirements.txt` 空；`run_update_cycle.py` 链为 compare→fetch→validate→pipeline→record_outcomes→(无 pending 时)predict，末打印 `Update cycle PASS`。
- 已 `git commit`（仅 README.md，L1 文档改动）：`git add README.md && git commit`。未提交项同前（数据主权排除）。

## 设计讨论：自我审视/自我进化架构（用户新需求，仅设计未实现）
- 用户诉求：让项目具备**自我审视 + 自我进化**能力，含**可视化迭代、审计、回溯**，以及**多专家团、多用户代表**，并**按需引入新专家团/技能**。明确要求"先完善想法与设计思路、集思广益，不要急着回答/实现"。
- 现有治理骨架已很硬：宪章第五条(5固定专家+≥4/5投票+高风险否决)、`docs/decisions/`不可变账本、`generate_evolution_proposal.py`(DRAFT 自动起草)、`run_update_cycle.py`(冻结盲评周期)、`reports/*-latest.json`、`backups/`。
- 关键澄清（已向用户指出）：在宪章红线内，"自我进化"只能=**自提案+自审计(沙箱内)**，实施仍走 Art.5 人工闸门；真正的自动实施需 L3 宪章修订(架构C)。
- 提出 3 种候选架构：A 受治理沙箱循环(推荐起点，已画架构图)、B 多面板审议引擎(分歧召募新专家)、C 宪法式自进化(需L3)。
- 两个核心注册表概念：Expert Registry(角色→职责→召募触发)、Skill Registry(能力→工具)；User Representatives 平行价值函数层(教育者/合规/探索者) operationalize 宪章第一条"学习而非获利"。
- 可视化四视图：演化主轴Spine / 专家·用户面板 / 实时要害Vitals / 差异·回溯Rollback(引入 System Version 整包回滚)。
- 待用户拍板 5 决策：①进化边界(A/B vs C) ②用户代表定义 ③可视化起步视图 ④回滚粒度 ⑤触发节奏。
- MVP 切片(零风险L0/L1)：建 `self_model.json` + Meta-Controller 周期后更新 + 提案挂 Expert Registry 路由 + 先做 Spine/Vitals 两视图。未动手，等用户定方向。

## 模型竞技场 / 诚实回测擂台（用户方向：多元描述性对照 + 置顶诚实层 + ML教学演示 + 假设可预测并修偏改进）
- 用户观察：每次预测结果几乎一模一样、且与回测实际偏差大。此前已诊断根因=近理想均匀随机（各位置频率±1.4%、7方法近12期0/12精确命中、主频率法 top1 跨11期冻结未变）。
- 本轮回应用户"假设可预测、用回测修偏改进"诉求，落地为**诚实假设检验擂台**（而非假装能预测），符合宪章第一条/第十条：
  - 新增 `workbench/predictive_eval.py`：严格扩展窗口评估器。对任意符合 REGISTRY `predict(train,top_k)` 契约的预测器，逐期仅用更早数据预测，计算精确命中/位命中/log-loss；与**均匀随机基线**(top_k/1000)做精确二项检验（纯stdlib，`math.lgamma`+方法小概率双侧 p + 右尾 exceeds_p），并对"优于基线"做 **Benjamini-Hochberg FDR(0.05) 多重比较校正**。
  - 新增 `workbench/arena_models.py`：4 个结构上异于既有 8 个同族频率法的新 challenger——`joint_trigram_frequency`(联合整体频率,非三位乘积)、`sum_conditioned_frequency`(先取最可能和值再排序)、`mean_reversion_bias_corrector`(预测近期欠出数字，可检验偏置修正)、`ml_logistic_teaching`(逐位多项逻辑回归，纯stdlib梯度下降，有界窗口250+缓存复用，严格时间前向)。
  - 大屏新增「模型竞技场」标签页：置顶诚实层横幅（基线命中率1%/理论log-loss 2.3026/与随机无差异数/FDR后仍优数）+ 多元描述性对照表（各方案族/精确率/基线期望/双侧p/校准log-loss/判定）+ 多重比较校正说明 + 修偏改进尝试（遍历窗口×混合系数12组）+ 免责声明。
  - 接入 `engine._run_predictive_arena` + `registry.predictive_arena` + `workbench.py predictive_arena` CLI + `collect_state`。
- **实测结论（last_n=200, top_k=10）**：11 方案中 6 个与基线无显著差异、1 个原始"显著优于"(FDR校正后 0 个)、4 个"显著劣于"(同为随机波动)。诚实层结论=**命中率与随机无异，无方案能在多重比较校正后稳定显著超越均匀随机基线**。修偏改进尝试最优者亦不超越基线。这与近均匀随机诊断一致，且经 FDR 校正避免了把单一随机正结果误判为信号（宪章第十条）。
- 性能：ML 方案用有界窗口+训练缓存后，last_n=200 全擂台约 55s（可跑）。默认 last_n=200、top_k=10。
- 待办：用户后续可要求提交 git 版本点（本回改 workbench 4 文件 + 新增 2 文件；未触碰权威库/数据语义，L1）。

## 辩论擂台扩展设计：激励+自进化+决策方向科学评价（仅设计，待用户确认）
- 用户（"对对对"）要求扩充 Task#32 双阵营辩论擂台：①引入激励/代币/积分；②探索AI自进化；③形成决策方向科学评价。明确"请扩充思路"(先设计后实现)。
- 召集 3 专家子代理设计三支柱并合成统一架构（闭环：自进化生成→独立审计→决策方向评分→激励账本奖惩→反馈下一代预算/权重）：
  - 支柱1 激励账本：AP(正方主张积分)/RP(反方纠错积分)+独立Reputation(0–100)。押金5/挑战费5；存活奖`50×merit_w`(merit_w=clamp(|h|×2+ln BF,0,1))、Rep+4；驳回没收+Rep−3；诚实撤回+20AP+Rep+2；反方正确驳回+30+10×decisiveness+Rep+3，错误驳回(主张存活)没收费+Rep−5；抓作弊赏200RP+Rep+10。护栏：押金即惩罚、诚信占优、驳回由独立审计裁定、声誉与积分分离、科学价值加权。落盘 `reports/incentive-ledger.json`。
  - 支柱2 自进化元循环：生成(mutate/compose+random_problem_gen+comprehensive特征交叉)→审计(FDR+安慰剂+半段)→缺口闭环(定向精修)→谱系(parent→child)→下一代。护栏：全局FDR递增惩罚`q_eff=q0·m0/m`、OOS盲窗(生成器不可见)、复用安慰剂+半段双闸、种子固定。收敛判据 D_g/S_g/家族衰减/连续T代random则停。谱系落盘 `claim_lineage.json`。
  - 支柱3 决策方向科学评价：关键区分 H+可预测 vs H0随机(H0不可证真，用TOST等价检验，结论须写"未检出可预测信号"非"已证明纯随机")。组合p(Fisher主+Stouffer稳健)+全组合FDR+组合BF(BF_port=ΠBF_i^w)；方向标量 `score=tanh(Z_port/√m)∈(−1,+1)` + CI(零分布置换校准，纯随机期望=0)；任何≥0阈值触发no_bet。输出决策方向块。
- 落地（待确认）：新增 `workbench/incentive_ledger.py`；扩展 `debate_arena.py`(元循环+方向分)；`dashboard.py` 三块可视化(账本/谱系收敛/方向仪表)；`engine/registry/workbench.py` 加 `evolution_arena` 命令。宪章合规：DB只读、时间前向、零泄漏、永不谎称可预测(内建于押金惩罚/OOS/TOST/no_bet)。
