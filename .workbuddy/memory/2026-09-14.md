# 2026-09-14

## 用户奠定「研究姿态约定」并据此改造进化引擎（反"躺平"）
- 用户原话核心：对任何事物不能以"无法预测"就开启躺平模式；要像预报天气那样，即便混沌难测也持续建模、追踪弱信号、寻找微妙联结与蝴蝶效应的蛛丝马迹。预埋"不可能"心理会在探索可能的因素与关联的逻辑决策链路上表现不佳，阻碍新文明与发现。必须像真正的科学家一样以开放心态探索，而不是预先关闭可能性。
- 据此识别并修复了引擎自身的两处"躺平"反模式：① `evolution_arena.run()` 中"连续两代 0 存活且 score≈0 则 break 停止生成"的收敛终止判据（已移除，仅保留 max_gens 硬上限）；② 结论措辞把"当前未检出"偷换为"与均匀随机不可区分/探索终止"（已改为开放科学姿态文本，disclaimer 同步强化）。
- 新增关系型/交互型特征原语以真正探索"微妙联结"：`FEAT_POOL` 扩展 `pos_pair`（两位同奇偶/相等/相差1）、`sum_digit`（和尾=某位）、`span_parity`（跨度奇偶=某位奇偶），并在 `_build_filter` 实现 + 保证 cross_seeds 回放可重建。
- 报告新增 `open_hypotheses`（lineage 中 status=near_miss 的条目），显式把近失主张框定为"仍在探索的开放假设"而非失败；`search_status` 恒为 `"open"`；`winner_label` 的 random 分支改为"当前未检出可区分信号 · 但探索持续开放（不预设不可预测）"。
- 大屏「进化擂台」：① 收敛说明改为"不以未检出信号为由停止生成"；② 新增「开放假设 · 持续探索中的弱信号」板块（`evolutionOpenHypotheses`）。
- 验证：reset 运行通过；search_status=open、open_hypotheses=6、关系型特征已出现（如 `('pos_pair','span','sum_tail')`、`('span_parity','span','sum_digit')`）；结论文本已改为开放姿态。
- 项目 MEMORY.md 新增「研究姿态约定」长期章节（用户明确的核心 operating principle）。
- 治理：改动限于结论措辞/探索循环终止逻辑/特征空间扩展与新增展示，未触碰宪章、数据契约、预测评估定义（FDR/安慰剂/复现/OOS/基线对照全部保留），属 L1；待确认后提交+推送。
