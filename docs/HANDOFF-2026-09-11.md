# 交接记录 / Handoff Record

> 格式遵循 `docs/AI_MEMORY_AND_HANDOFF.md` 最小格式。本记录使本次会话的全部操作可追溯。

- **handoff_id**: HANDOFF-2026-09-11
- **charter_version**: 1.0.0（根目录 `PROJECT_CHARTER.md`）
- **agent**: WorkBuddy（AI 助手）
- **date**: 2026-09-11
- **scope**:
  - 通读项目治理文档（`PROJECT_CHARTER.md` + `docs/` 全部 11 篇 + `docs/decisions/` 4 文件）
  - 修复 `frozen-*.json` glob 误匹配导致的三处缺陷
  - 新增只读提取脚本 `extract_predictions.py`
  - 归档游离脚本 `3D.py` → `scratch/`
  - 删除 5 个嵌套垃圾 comparison 文件
  - 全程提交至 git 并产出本交接记录
- **read_files**:
  - `PROJECT_CHARTER.md`
  - `docs/`: PROJECT_ARCHITECTURE / DATA_PERSISTENCE / AI_MEMORY_AND_HANDOFF / QUICKSTART / FETCH_SAFETY / PREDICTION_MODELING_GOVERNANCE / CENTRAL_BRAIN_MODEL_SELECTION / DIGITAL_BRAIN_DESIGN / ANALYSIS_METHOD_CATALOG / TEACHING_AND_OBSERVABILITY / MODEL_PROPOSAL_TEMPLATE
  - `docs/decisions/`: README / TEMPLATE / EVOLUTION-DRAFT / MODEL-SCREENING-AND-BRAIN-V2-PROPOSAL
  - 脚本: `fetch_sd3d.py` `run_update_cycle.py` `scan_leakage.py` `record_outcomes.py` `compare_prediction.py` `extract_predictions.py`（自建）`3D.py`（归档）`data.csv`
- **current_data_range**: `draws` 表 **7750 行**，期号 **2004001 → 2026243**，最新开奖日 **2026-09-10**
- **current_db_hash**: `ab16c01a401087f63656fa73d111ec9b353f7273c0028256b4aac12d6fcce9da`（sha256 of `sd3d_history.sqlite3`）
- **current_tx_tail_hash**: `a6eb70ec445d25d0faa8823fd270ffe90d418ad9926cacb841ee0c4ca1b233ec`（`sd3d_history.jsonl` 末行 `event_hash`，type=run_commit，7758 行）
- **uncommitted_changes**: 本记录随附一次 git 提交，包含下列改动（提交前工作区其余未跟踪项见 `known_risks`）
- **known_risks**:
  1. `fetch_sd3d.py`、`run_pipeline.py` 的既有未提交改动**非本会话产生**（分别为快照二进制写入修复、接入 `model_screening.py`），本次一并提交但出处不同，已在本记录标注。
  2. `predictions/extracted_predictions.csv` 为可重建导出，非权威数据源。
  3. `docs/decisions/MODEL-SCREENING-AND-BRAIN-V2-PROPOSAL.md` 仍为 `DRAFT`，未走专家审计投票，不生效。
  4. `model_gate` 状态 `BASELINE_REQUIRED`：任何模型晋级须先完成专家审计+投票。
- **next_safe_action**: 待 2026-09-11 开奖（期 2026244）放出后，运行 `python run_update_cycle.py` 即可自动对比本期并冻结下一期；新冻结文件 `frozen-2026244-*.json` 当前为 `pending`。

---

## 操作日志（可追溯明细）

| # | 操作 | 文件 | 改动 | 验证 | 风险等级 |
|---|------|------|------|------|----------|
| 1 | 修复 glob 误匹配 | `run_update_cycle.py` L25-27 | `glob("frozen-*.json")` → 过滤 `"-comparison" not in name` | 重跑全流程 `Update cycle PASS` | L1 |
| 2 | 修复 glob 误匹配 | `scan_leakage.py` L24-26 | 同上过滤 | `Leakage scan: PASS; scanned=2` | L1 |
| 3 | 修复 glob 误匹配（崩溃根因） | `record_outcomes.py` L30-32 | 同上过滤（原 L34 `artifact["run_id"]` 因 comparison 文件无 run_id 抛 KeyError） | `Outcome archive: 2 completed` | L1 |
| 4 | 新增只读提取器 | `extract_predictions.py`（新建） | 扫描 `frozen-*.json`、关联 `draws` 表、导出 CSV；自带 `comparison`/`kind` 跳过 | 实跑导出 2 期（172/935 均未命中） | L1 |
| 5 | 归档游离脚本 | `3D.py` → `scratch/3D.py` | `git mv` 保留历史；空 `data.csv` 一并挪至 `scratch/` | 根目录已无 `3D.py`；全项目仅 `3D.py` 引用 `data.csv` | L1 |
| 6 | 删除垃圾产物 | `predictions/*-comparison-comparison*.json`（5 个） | `rm`（2 层及以上嵌套，纯 bug 产物） | 保留 2 真实冻结 + 2 合法 1 层 comparison | L1（删除，已确认） |
| 7 | 提交 + 交接 | git + `docs/HANDOFF-2026-09-11.md` | 见 commit | `git status` 干净（除已知未跟踪项） | — |

**回滚方案**：
- 代码修复：git revert 对应 commit 即可恢复三处 `glob` 过滤。
- 归档：`git mv scratch/3D.py 3D.py` 还原（或 `git show <commit>:3D.py > 3D.py`）。
- 删除的 5 个嵌套文件：非权威数据、可重建（重跑 `run_update_cycle.py` 会再生成 1 层 comparison；2 层+ 仅由旧 bug 产生，无需恢复）。

**验证命令**（复现用）：
```bash
python run_update_cycle.py          # 期望: Update cycle PASS
python scan_leakage.py --db sd3d_history.sqlite3   # 期望: PASS; scanned=2
python extract_predictions.py       # 重新导出 predictions/extracted_predictions.csv
```
